config={}

config.update(APP_NAME='django-start') #used as container folder in /tmp folder for some tasks
config.update(APP_HOME='/home/namgivu/NN/code/django-start') #the root folder of your application

util_config=config